/*Except Safari, make bigger checkbox and radion*/
input[type='radio'],
input[type='checkbox'] {
  width: 3rem;
  height: 3rem;
  accent-color: green;
}

/*A link without an href attribute is the only way to disable a link. */
/*When a link has no href, it has no role, no focusability, and no keyboard events.*/
/*download instead of open it*/
a:not([href]) {
  /* style a "disabled" link */
}

/* Style all links that include .pdf at the end */
a[href$='.pdf']::after {
  content: ' (PDF)';
}

/*multiple submit button with its own method, action, formenctype, can even put outside a form*/

button[disabled],
[disabled] {
  opacity: 0.5;
  pointer-events: none;
}

.button,
.linkBtn {
  border: 0;
  border-radius: 0.25rem;
  background: #1e88e5;
  color: white;
  font-family: -system-ui, sans-serif;
  font-size: 1rem;
  line-height: 1.2;
  white-space: nowrap;
  text-decoration: none;
  padding: 0.25rem 0.5rem;
  margin: 0.25rem;
  cursor: pointer;
}

/*
https://css-tricks.com/responsive-layouts-fewer-media-queries
https://ishadeed.com/article/defensive-css/
*/

/*focus*/
:is(a, button, input, textarea, summary) {
  --outline-size: max(2px, 0.08em);
  --outline-style: solid;
  --outline-color: currentColor;
}
:is(a, button, input, textarea, summary):focus {
  outline: var(--outline-size) var(--outline-style) var(--outline-color);
  outline-offset: var(--outline-offset, var(--outline-size));
}
:is(a, button, input, textarea, summary):focus-visible {
  outline: var(--outline-size) var(--outline-style) var(--outline-color);
  outline-offset: var(--outline-offset, var(--outline-size));
}
:is(a, button, input, textarea, summary):focus:not(:focus-visible) {
  outline: none;
}
